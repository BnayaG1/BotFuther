STEP 1 of 5 — [STEP_1: BEAM_GEOMETRY]. Output JSON only. Do NOT extract supports or loads.

{pipeline_intro}

{handwriting_hint}

LINEAR SCAN — Step 1 only: read dimension chain left→right; establish L and labeled_points.

Task:
1) Rotate mentally so the beam is horizontal left→right.
2) Read the dimension chain (tick marks + numbers) along the beam axis.
3) x=0 at the leftmost tick on the dimension chain (left end of beam axis).
4) List every labeled point (C, A, D, E, F, B...) with cumulative x [m].
5) L = sum of ALL segment lengths = x of rightmost beam end.

Output ONLY this schema:
{{
  "STEP_1_BEAM_GEOMETRY": {{
    "exercise_type": "beam" | "cog" | "unknown",
    "confidence": "high" | "medium" | "low",
    "image_focus": {{
      "exercise_region": "upper" | "middle" | "full",
      "ignored_regions": "Hebrew: scratch work ignored"
    }},
    "L": number,
    "origin": "left_end_of_beam_axis",
    "left_end_label": "string",
    "right_end_label": "string",
    "segments": [
      {{"from_label":"C","to_label":"A","from_x":0,"to_x":4.2,"length_m":4.2}}
    ],
    "labeled_points": [
      {{"label":"C","x":0}},
      {{"label":"A","x":4.2}}
    ],
    "key_points_m": [0, 4.2, 8.2]
  }}
}}

Rules:
- Units: meters only. sum(segments.length_m) MUST equal L.
- COG exercise → exercise_type=cog, minimal geometry.
- Ignore student equilibrium calculations in margins.
{extra}
