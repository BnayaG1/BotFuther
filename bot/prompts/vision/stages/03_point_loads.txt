STEP 3 of 5 — [STEP_3: POINT_LOADS]. Output JSON only. NO distributed loads in this step.

{pipeline_intro}

{handwriting_hint}

Prior context:
{context}

Task: extract concentrated forces and concentrated moments ONLY.
Types: point, moment, inclined — never type distributed here.

CRITICAL rules:
- LOAD x: load center on beam → vertical projection to dimension line → that x wins. Set label_at when tick hit.
- VERTICAL ↓/↑: type=point, Fy only, Fx=0. NEVER inclined. Do not invent Fx/angle for vertical arrows.
- AXIAL ←/→: type=point, Fx only, Fy=0. NEVER inclined.
- INCLINED (clearly slanted ↘/↙ only, usually with 30/45/60 written):
  • type MUST be "inclined" — never "point" with only Fy when truly slanted.
  • REQUIRED: magnitude_ton, angle_deg, incl_dir, plus Fx, Fy.
  • α from horizontal as written — do not swap 30°↔60°.
  • Fx=F·cos(α), Fy=F·sin(α); dl⇒Fx<0; dr⇒Fx>0. Arrow TIP on beam = x.
- Moment: center of curved arrow on beam. If the arc is at B / on the roller, keep x at B; if at F, keep F.
- At one label (e.g. C): BOTH moment AND inclined allowed at same x.
- Horizontal ← at end is Fx negative, Fy=0.
- DUAL UNITS: "100kn (10t)" ↓ → Fy=10.
- Counter-clockwise moment → positive M.
- Sort loads[] by ascending x.

Output ONLY:
{{
  "STEP_3_POINT_LOADS": {{
    "loads": [
      {{
        "type": "point",
        "x": number,
        "Fy": number,
        "Fx": number_or_null,
        "direction": "left" | "right" | "down" | "up",
        "label_at": "D"
      }},
      {{
        "type": "moment",
        "x": number,
        "M": number,
        "label_at": "F"
      }},
      {{
        "type": "inclined",
        "x": number,
        "magnitude_ton": number,
        "angle_deg": number,
        "incl_dir": "dl" | "dr",
        "Fx": number,
        "Fy": number,
        "label_at": "C"
      }}
    ]
  }}
}}
{extra}
