HANDWRITING extras (apply with the extraction spec):
- Prefer the diagram over scribbles; do not invent equal segment lengths.
- Dimension numbers between ticks are lengths [m], not load magnitudes.
- Dual units "100kn (10t)" → use 10 ton. Bare kN only → ÷10 to ton.
- Same x may hold moment + inclined + vertical + axial — list each separately in loads[].
- Horizontal ←/→ on the beam (including midspan) is Fx axial (Fy=0), not a vertical load and not inclined.
- If a value is unreadable, add uncertainties[].question_he in Hebrew; else uncertainties=[].
