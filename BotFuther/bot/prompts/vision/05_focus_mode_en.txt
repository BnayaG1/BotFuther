FOCUS MODE: Step 1 — read bottom dimension line ONLY; cumulative-sum every segment (1+1+3+...) for exact x of A,C,D... — no visual guessing. Step 2 — for each load: center on beam → vertical to dimension line → that x. Phase A — L + supports before loads. Phase B — left→right; one UDL per span. Dimension chain ALWAYS wins. Output loads[] in ascending x order. Focus on the UPPER exercise diagram. Ignore scratch calculations.
