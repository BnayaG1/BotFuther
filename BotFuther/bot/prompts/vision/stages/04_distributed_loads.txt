STEP 4 of 5 — [STEP_4: DISTRIBUTED_LOADS]. Output JSON only. NO point/moment/inclined in this step.

{pipeline_intro}

{handwriting_hint}

Prior context:
{context}

Task: extract distributed loads ONLY — follow distributed_loads_protocol below.

{distributed_loads_protocol}

BRACKET RULE: horizontal line above beam with multiple vertical arrows = distributed even if label is "3t" without /m.
Do NOT duplicate as point loads at span boundaries.

Output ONLY:
{{
  "STEP_4_DISTRIBUTED_LOADS": {{
    "distributed_loads": [
      {{"start_x": number, "end_x": number, "magnitude": number, "shape": "rectangular" | "triangular"}}
    ]
  }}
}}
{extra}
