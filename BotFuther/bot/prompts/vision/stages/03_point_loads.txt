STEP 3 of 5 — [STEP_3: POINT_LOADS]. Output JSON only. NO distributed loads in this step.

{pipeline_intro}

{handwriting_hint}

Prior context:
{context}

Task: extract concentrated forces and concentrated moments ONLY.
Types: point, moment, inclined — never type distributed here.

CRITICAL rules:
- LOAD x: load center on beam → vertical projection to dimension line → that x wins. Set label_at when tick hit.
- INCLINED loads (angled arrow, NOT vertical):
  • type MUST be "inclined" — never "point" with only Fy when the arrow is slanted.
  • α = acute angle from horizontal (+x) in degrees — read the number on the diagram (30, 45, 60).
    30° is NOT 60° — do not use the complementary angle.
  • Decompose: Fx=F·cos(α), Fy=F·sin(α); Fy always positive (downward component).
  • incl_dir: "dl" (↙ down-left, Fx negative) or "dr" (↘ down-right, Fx positive).
  • Arrow TIP on beam = x.
- Moment: center of curved arrow on beam — NOT at roller symbol B if arrow is at F (project to F).
- At one label (e.g. C): BOTH moment AND inclined allowed at same x.
- Horizontal ← at end is Fx negative, Fy=0.
- DUAL UNITS: "100kn (10t)" ↓ → Fy=10.
- Counter-clockwise moment → positive M.
- Sort loads[] by ascending x.

Output ONLY:
{{
  "STEP_3_POINT_LOADS": {{
    "loads": [
      {{
        "type": "point",
        "x": number,
        "Fy": number,
        "Fx": number_or_null,
        "direction": "left" | "right" | "down" | "up",
        "label_at": "D"
      }},
      {{
        "type": "moment",
        "x": number,
        "M": number,
        "label_at": "F"
      }},
      {{
        "type": "inclined",
        "x": number,
        "Fx": number,
        "Fy": number,
        "angle_deg": number,
        "incl_dir": "dl" | "dr",
        "label_at": "C"
      }}
    ]
  }}
}}
{extra}
