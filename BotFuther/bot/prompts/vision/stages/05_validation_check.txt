STEP 5 of 5 — [STEP_5: VALIDATION_CHECK]. Output JSON only. Do NOT re-extract — verify prior steps.

{pipeline_intro}

Prior context (all steps 1–4):
{context}

Task: completeness check only — no math, no reactions.
Verify:
1) L > 0 and segment sum ≈ L.
2) Every support x in [0, L].
3) Every point load / moment x in [0, L].
4) Every distributed start_x/end_x in [0, L] and end_x > start_x.
5) No required field empty (L, supports if drawn, loads if arrows visible).
6) No duplicate: same force both in point_loads and distributed_loads.

Output ONLY:
{{
  "STEP_5_VALIDATION_CHECK": {{
    "is_complete": true | false,
    "issues": ["Hebrew: list every problem found"],
    "empty_fields": ["e.g. STEP_4_DISTRIBUTED_LOADS.distributed_loads missing when bracket drawn"],
    "geometry_conflicts": ["e.g. load at x=17 when L=16.2"]
  }}
}}
{extra}
